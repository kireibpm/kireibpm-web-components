# Temporary fallback for environments with TLS interception.
# Preferred long-term setup:
#   ./infrastructure/configure-npm-ca.sh --cafile=/absolute/path/to/company-root-ca.pem --scope=components
strict-ssl=false